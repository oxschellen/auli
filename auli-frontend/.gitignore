.claude
.vscode

.env
.env.*
notas.txt
web_config.txt

/*.txt
/*.env
/*.pem
/*.key
/*.crt

/dist
/node_modules

# Dados por entidade em public/<id>/ são GERADOS de data/ (scraper → collections → build-frontend-public)
# e ficam fora do git — só os assets do app na raiz de public/ (about.md, favicon, robots.txt, sitemap.xml)
# são versionados. Repor com `scripts/build-frontend-public.sh` antes do build/deploy do frontend.
public/*/
